



# improvement(VBS) - улучшение
# improvement(SQL)
#
# addFile(SQL) просто добаление данных, тоже что и improvement(SQL)
# updFile(SQL) обновление файла
# fix(SQL) исправление ошибок
# feat(SQL) новая функция в коде
# refCode(SQL) рефакторинг кода, переработка, усовершенствование
# chore(PY) простая рутина
# docs(PY) добавление описания
# style(SQL) стайл-кодинг
# test(PY) добавление тестов
# perf(SQL) улучшение производительности
#
# BREAKING CHANGE: коммит, который имеет сноску BREAKING CHANGE или коммит, заканчивающийся восклицательным знаком (!) после типа или контекста, 
#                 вводящий изменение(я), нарушающие обратную совместимость (соответствует MAJOR в Cемантическом Версионировании). 
#                 BREAKING CHANGE может быть частью коммита любого типа.